VITE_SUPABASE_URL=https://TU_PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_public_key

# Usuario de desarrollo local (solo usado en npm run dev, nunca en producción)
# Prerrequisito: en Supabase → Authentication, habilita el proveedor Email y
# define una contraseña para tu cuenta de Google (mismo user_id, conserva admin y pronósticos).
VITE_DEV_EMAIL=
VITE_DEV_PASSWORD=
